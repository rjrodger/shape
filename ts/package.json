{
  "name": "shape",
  "version": "11.4.1",
  "description": "An object shape validation utility.",
  "main": "dist/shape.js",
  "browser": {
    "util": "./src/web/util.js"
  },
  "type": "commonjs",
  "types": "dist/shape.d.ts",
  "homepage": "https://github.com/rjrodger/shape",
  "keywords": [
    "shape",
    "schema",
    "validation"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/rjrodger/shape.git"
  },
  "scripts": {
    "test": "node test/run.js",
    "test-some": "node test/run.js --test-name-pattern",
    "build": "npm run version && tsc --build src test && npm run build-web",
    "build-web": "esbuild src/shape.web.js --bundle --minify --keep-names --format=iife --global-name=Shape --platform=browser --alias:util=./src/web/util.js --footer:js=\"if(typeof module!=='undefined'&&module.exports)module.exports=Shape;\" --outfile=dist/shape.min.js --log-level=warning",
    "watch": "npm run version && tsc --build src test -w",
    "version": "node -r fs -e \"v=require('./package.json').version;s=fs.readFileSync('./src/shape.ts').toString();if(!s.includes('VERSION = \\''+v+'\\'')){s=s.replace(/VERSION = '.*?'/,'VERSION = \\''+v+'\\'');fs.writeFileSync('./src/shape.ts',s)}\"",
    "clean": "rm -rf node_modules yarn.lock package-lock.json dist dist-test",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag && npm publish --access public --registry https://registry.npmjs.org "
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "files": [
    "README.md",
    "src",
    "dist",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/node": "26.1.1",
    "esbuild": "^0.28.2",
    "typescript": "7.0.2"
  }
}
